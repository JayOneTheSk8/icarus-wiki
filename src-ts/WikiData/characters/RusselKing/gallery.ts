import { GallerySection } from '../../../DataTypes'
import { imgurLink, GALLERY } from '../../../constants'

const gallery: GallerySection = {
    title: GALLERY,
    gallery: [
        {
            url: imgurLink('iFCQCth')
        }
    ]
}

export default gallery
